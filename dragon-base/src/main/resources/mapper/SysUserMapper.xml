<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gmtok.dragon.base.mapper.SysUserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.gmtok.dragon.base.entity.SysUser">
        <id column="id" property="id" />
        <result column="user_code" property="userCode" />
        <result column="user_name" property="userName" />
        <result column="password" property="password" />
        <result column="user_type" property="userType" />
        <result column="phone" property="phone" />
        <result column="email" property="email" />
        <result column="is_disabled" property="isDisabled" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id AS id, user_code AS userCode, user_name AS userName, password AS password, user_type AS userType, phone AS phone, email AS email, is_disabled AS isDisabled
    </sql>

</mapper>
